name = "MOD_NAME"
author = "Andx"
prefix = "PREFIX"
mainprefix = "z"

[version]
major = 0
minor = 1
patch = 0
build = 0
git_hash = 0
# TEMPLATE: once you scaffold addons/main (`hemtt new`), switch to tracking
# the version from addons/main/script_version.hpp instead, e.g.:
# path = "addons/main/script_version.hpp"

[files]
include = [
    "mod.cpp",
    "meta.cpp",
    "LICENSE",
    "README.md",
]
